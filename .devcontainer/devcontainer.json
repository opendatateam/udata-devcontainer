{
    "dockerComposeFile": "../docker-compose.yml",
    "workspaceFolder": "/workspace",
    "service": "udata",
	"extensions": [
		"ms-python.python",
        "octref.vetur"
	],
    "forwardPorts": [7000],
    "postCreateCommand": "pip install -e udata && pip install -e udata-front && echo 'Setup is ready!'"
}
